# tuples are almost same as lists , but tuples are not mutable,
# the iteration in tuples is faster in that case

# tup = ()

tup1 = (1,2,3,4)

print(tup1)

print(tup1[2])

# these have only two operations, count and index

# set, s= {}, in this there is no indexing possible as set gives out random sequence of
# elements whenever tried to print

s1= {1,2,3,4,5}
print((s1))